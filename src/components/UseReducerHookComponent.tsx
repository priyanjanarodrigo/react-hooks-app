import { ReactElement } from "react";
import '../styles/use-reducer-hook-component-styles.scss'

const UseReducerHookComponent: React.FC = (): ReactElement => {
    return (
        <div id="use-reducer-wrapper">
            <div>
                <p>useReducer Hook</p>
            </div>
        </div>
    );
};

export default UseReducerHookComponent;