import { ReactElement } from "react";

const UseReducerToDoComponent: React.FC = (): ReactElement => {
    return (
        <div>
            <h3>useReducer ToDo Component</h3>
        </div>
    )
}

export default UseReducerToDoComponent;